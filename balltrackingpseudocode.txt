stream <- capture video

while True:
    frame <- read stream
    frame <- resize to width = 600
    blurred <- blur frame
    hsv <- bgr to hsv
    mask <- all colors in color range
    mask <- erode blobs from mask twice
    mask <- dilate mask twice
    circles <- circles detected

    if circles is not empty:
        draw circles to frames
        centers_list <- add centers

    show frames

    if q is pressed:
        quit()

stop video stream
destroy all windows
